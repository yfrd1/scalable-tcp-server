
#include "auth_service.hpp"
#include "session/session.hpp"
#include "common/packet.hpp"

namespace scalable {
namespace server {

    void AuthService::loginService(
      Session& session, Packet& packet)
    {

    }
    
    void AuthService::registerService(
      Session& session, Packet& packet)
    {

    }
    
}
}